class NoteforListing {
  String noteID;
  String noteTitle;
  DateTime createDateTime;
  DateTime latestEditedTime;

  NoteforListing(
      {this.noteID,
      this.noteTitle,
      this.createDateTime,
      this.latestEditedTime});
}
